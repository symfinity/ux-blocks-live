<div
    data-ui-role="tabs"
    data-ui-fragment="blocks.live.tabs"
    data-controller="symfony--ux-blocks-live--tabs"
    data-symfony--ux-blocks-live--tabs-orientation-value="{{ orientation }}"
    data-symfony--ux-blocks-live--tabs-default-value-value="{{ defaultValue }}"
    {{ attributes }}
>
    {% block content %}{% endblock %}
</div>

<div
    data-ui-role="hover-card"
    data-ui-fragment="blocks.live.hover-card"
    data-controller="symfony--ux-blocks-live--hover-card"
    data-symfony--ux-blocks-live--hover-card-open-delay-value="{{ openDelay }}"
    data-symfony--ux-blocks-live--hover-card-close-delay-value="{{ closeDelay }}"
    {{ attributes }}
>
    {% block content %}{% endblock %}
</div>

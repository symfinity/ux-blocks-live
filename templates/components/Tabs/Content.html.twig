<div
    role="tabpanel"
    data-ui-role="tabs-content"
    data-value="{{ value }}"
    {% if not active %}hidden{% endif %}
    aria-hidden="{{ active ? 'false' : 'true' }}"
    data-symfony--ux-blocks-live--tabs-target="panel"
    {{ attributes }}
>
    {% block content %}{% endblock %}
</div>
